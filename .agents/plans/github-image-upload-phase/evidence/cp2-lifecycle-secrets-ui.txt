CP2 - lifecycle / secret-boundary / UI checkpoint

- Focused verification:
  - `pnpm exec vitest run tests/http-server.test.ts tests/naver-blog-exporter.test.ts tests/ui/export-options-panel.test.tsx tests/ui/use-export-job.test.tsx tests/ui/app.test.tsx --silent`
  - Result: passed (`5` files, `27` tests)
- Manual checkpoint notes:
  - `tests/http-server.test.ts` asserts completed and failed polled job payloads do not contain `providerFields`, `owner/name`, or token values after upload attempts.
  - `tests/ui/use-export-job.test.tsx` and `tests/ui/app.test.tsx` now cover accepted upload polling, rejected upload request rollback, upload-failed retry, upload-only mode, and zero-candidate no-form behavior.
  - Bundle evidence refreshed for T6/T7 no longer contains stale raw-JSON UI claims. Current completed-task evidence (`t4`-`t7`) contains no secret values.
  - Older T8/T9 smoke/doc artifacts are still pending refresh and will be replaced in the next task; they were not used as CP2 signoff evidence.
