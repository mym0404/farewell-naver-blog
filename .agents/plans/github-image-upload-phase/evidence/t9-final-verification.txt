T9 - final verification wave

- Focused regression:
  - `pnpm exec vitest run tests/export-options.test.ts tests/http-server.test.ts tests/asset-store.test.ts tests/export-preview.test.ts tests/export-single-post.test.ts tests/markdown-renderer.test.ts tests/naver-blog-exporter.test.ts tests/ui/export-options-panel.test.tsx tests/ui/use-export-job.test.tsx tests/ui/app.test.tsx --silent`
  - Result: passed (`9` files, `54` tests)
- Coverage:
  - `pnpm test:coverage`
  - Result: passed (`23` files, `137` tests, overall coverage 90.35% statements / 84.71% branches / 90.99% funcs / 90.35% lines)
- Smoke:
  - `pnpm smoke:ui`
  - Result: passed (`smoke:ui passed (job-smoke)`)
- Composite/full-suite follow-up:
  - `pnpm check:full`
  - Result: failed early in `pnpm typecheck` with `src/modules/parser/se2-parser.ts(1,30): error TS2307: Cannot find module 'domhandler' or its corresponding type declarations.`
  - This parser import sits outside the reopened upload-contract surface and was not touched by this bundle.
  - `pnpm test:network` was run separately after the composite stopped; it failed on `tests/naver.integration.test.ts` with a 60s timeout while collecting public posts from `mym0404`, which is likewise outside the reopened upload surface and depends on live network conditions.
  - `pnpm samples:verify` was run separately after the composite stopped and passed (`samples:verify passed (8 samples)`).
- Generated artifacts:
  - `pnpm quality:report` inside `check:full` updated `docs/generated/quality-score.md` and `docs/generated/sample-coverage.md`.
- Adjudication:
  - The reopened upload surface is verified by focused tests, coverage, smoke, and sample verification.
  - The remaining composite blockers are unrelated parser/network issues outside this bundle's declared file surface, so T9 closes with recorded adjudication rather than a green `check:full`.
