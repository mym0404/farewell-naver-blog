T4 verification
Timestamp: 2026-04-17 06:03:32Z

Changed files
- src/server/http-server.ts
- tests/http-server.test.ts
- .agents/plans/github-image-upload-phase/tasks.md

Command
- pnpm exec vitest run tests/http-server.test.ts tests/naver-blog-exporter.test.ts --silent

Result
- PASS (13 tests, 2 files)

Notes
- Upload request body now expects `providerKey + providerFields` instead of `uploaderConfigJson`.
- Same-origin guard now rejects `Origin`-missing upload POSTs and still requires `X-Requested-With: XMLHttpRequest`.
- Upload failures preserve a redacted reason in job logs/error instead of collapsing every cause to one opaque fixed string.
- `upload-failed` jobs can re-enter the same upload endpoint for retry, and polling payloads still do not serialize provider fields or token values.
- Live diff stayed within the declared T4 write surface plus bundle execution bookkeeping in `tasks.md`.
