T6 - UI options / upload-phase entry

- Changed files:
  - `src/ui/App.tsx`
  - `tests/ui/export-options-panel.test.tsx`
  - `tests/ui/app.test.tsx`
- Assets tab keeps the new `imageHandlingMode`, `compressionEnabled`, and `groupByCategory` contract without rendering any post-export provider form.
- Upload-phase mode now stays active for `upload-ready`, `uploading`, `upload-failed`, and `upload-completed`, so category/export setup panels remain hidden throughout the same-job upload flow.
- Verification:
  - `pnpm exec vitest run tests/ui/export-options-panel.test.tsx tests/ui/app.test.tsx --silent`
  - Result: passed (`2` files, `9` tests)
