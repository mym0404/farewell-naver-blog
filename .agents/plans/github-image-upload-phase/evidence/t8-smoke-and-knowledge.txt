T8 - knowledge / smoke sync

- Updated knowledge and runbook artifacts:
  - `.agents/knowledge/product/domain.md`
  - `.agents/knowledge/architecture/system-map.md`
  - `.agents/knowledge/product/ui-dashboard-design-system.md`
  - `docs/runbooks/browser-verification.md`
  - `scripts/harness/run-ui-smoke.ts`
- Docs now describe:
  - per-post `index.md` output layout
  - `download-and-upload` same-job lifecycle with `upload-ready -> uploading -> upload-failed -> retry -> upload-completed`
  - post-export-only provider form using structured `providerKey/providerFields`
  - upload-only mode keeping setup panels hidden on desktop and mobile
- Smoke now mocks the structured provider payload, validates failed-upload retry before completion, preserves placeholder-only provider values, and checks the mobile upload table plus hidden setup panels during upload-only mode.
- Verification:
  - `pnpm smoke:ui`
  - Result: passed (`smoke:ui passed (job-smoke)`)
