T7 - UI results / structured provider flow

- Changed files:
  - `src/ui/hooks/use-export-job.ts`
  - `src/ui/features/job-results/job-results-panel.tsx`
  - `src/ui/App.tsx`
  - `tests/ui/use-export-job.test.tsx`
  - `tests/ui/app.test.tsx`
- Replaced the raw upload JSON form with a post-export provider form that submits `providerKey` plus structured `providerFields` from the shared results panel.
- `useExportJob` restores the previous `upload-ready` job when the upload request is rejected, while keeping polling continuity through accepted `uploading` and terminal upload states.
- Smoke follow-up reopened T7 once more to restart polling after `upload-failed` same-job retry; focused hook/app tests now cover that regression explicitly.
- The results panel keeps the upload target table visible, preserves upload-only mode, and allows same-job retry from `upload-failed` with corrected provider fields.
- Verification:
  - `pnpm exec vitest run tests/ui/use-export-job.test.tsx tests/ui/app.test.tsx tests/ui/export-options-panel.test.tsx --silent`
  - Result: passed (`3` files, `13` tests)
