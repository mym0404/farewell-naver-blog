All non-final acceptance criteria are met: BaseBlock has outputOptions; registry derives selectable Editor+Block entries and static NaverBlog definitions; UI renders grouped Editor+Block controls and writes stable keys; settings/API/CLI sanitize and validate only Editor+Block keys; exporter passes blockOutputs into parsing; BaseEditor stamps selectable AST blocks; MarkdownRenderer consumes per-block selections; manifest and resume snapshots keep Editor+Block options; fixture/smoke/tests cover the public flow. Reopen conditions are inactive: no direct UI block family usage, no block-type-only manifest defaults, no same-block collapse, and no compatibility layer for old keys.
