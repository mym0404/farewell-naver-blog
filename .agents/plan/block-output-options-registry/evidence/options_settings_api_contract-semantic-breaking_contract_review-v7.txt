pass: ExportOptions.blockOutputs.defaults is now typed as a map keyed by EditorBlockOutputSelectionKey. cloneExportOptions builds defaults from getEditorBlockOutputDefinitions(new NaverBlog()), so emitted defaults are stable editor:block keys such as naver-se4:formula. sanitizePersistedExportOptions filters blockOutputs.defaults by the same registry key set and drops block-type-only keys like code. The single-post CLI builds its allowed key and variant map from getEditorBlockOutputDefinitions(new NaverBlog()) and assertAllowedKeys rejects blockOutputs.defaults.code. No migration or fallback path accepts old public block-type-only payloads.
