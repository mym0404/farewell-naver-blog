Smoke now checks the naver-se4:formula card, waits for /api/export-settings to persist defaults["naver-se4:formula"].params.inlineWrapper, rejects /api/export payloads missing that key or containing old formula, echoes captured ExportOptions into job and manifest responses, and verifies the retrieved manifest preserves the same Editor+Block key. Parser and renderer tests cover naver-se4:code versus naver-se3:code independence, exporter and resumable manifest tests assert naver-se4:code, and HTTP settings tests assert naver-se4:formula in persisted bootstrap payloads.
