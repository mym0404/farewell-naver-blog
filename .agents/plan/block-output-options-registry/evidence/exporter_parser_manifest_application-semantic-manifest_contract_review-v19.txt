createInitialManifest stores the cloned ExportOptions object on manifest.options, and NaverBlogExporter writes that manifest.json without remapping blockOutputs. buildResumableExportManifest keeps options from job.request.options and keeps the same request under manifest.job.request, while JobStore.hydrate restores manifest.job.request unchanged. Exporter and resumable manifest tests assert naver-se4:code is present and old block-type-only code is absent from the active options/defaults contract. Existing HTTP/settings sanitization rejects old keys before jobs are created, so resume snapshots preserve the Editor+Block shape end to end.
