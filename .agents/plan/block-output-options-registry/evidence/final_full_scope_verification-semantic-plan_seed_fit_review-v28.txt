Final implementation matches the approved seed: BaseBlock owns outputOptions, NaverBlog editors expose typed identity and supportedBlocks order, BlockRegistry exposes stable Editor+Block definitions, UI/settings/API/exporter/manifest use Editor+Block keys, parser stamps selectable blocks with editor-specific outputSelection, and smoke/tests cover the requested UI-to-export path. Non-goals are respected: there is no old settings/API/manifest migration or compatibility layer, no live network run was added, and fixture Markdown was not rewritten without an outputOptions reason.
