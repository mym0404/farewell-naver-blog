pass: /api/export-defaults emits cloneExportOptions(), whose blockOutputs.defaults map is built from getEditorBlockOutputDefinitions(new NaverBlog()). /api/export-settings sanitizes payload.options with sanitizePersistedExportOptions, then validates with cloneExportOptions before writePersistedUiState persists the sanitized payload. /api/export calls cloneExportOptions(payload.options), so server internal export requests receive the same ExportOptions shape. The single-post CLI validates blockOutputs.defaults against the same getEditorBlockOutputDefinitions(new NaverBlog()) registry map. UI Api types import ExportOptions directly, so bootstrap/settings/export payload typing shares the same Editor+Block key contract.
