BlockOutputCard resolves and writes with selectionKey: family.key, stores updates at blockOutputs.defaults[family.key], and keeps the real stable key in data-block-output-card while using only a sanitized copy for DOM ids. The onOptionsChange path still flows through the existing ExportOptions object, so UseExportSettingsSync and API payload handling continue to persist/send the new typed blockOutputs.defaults map without a separate UI adapter. The preview update test changes naver-se4:code, asserts defaults["naver-se4:code"] is set, asserts defaults["naver-se3:code"] remains undefined, then re-renders from latestOptions and sees the SE4 preview update. The smoke selector was moved to naver-se4:formula and pnpm smoke:ui passed.
