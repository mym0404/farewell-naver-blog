Regression review: changes are scoped to outputOptions ownership, Editor+Block settings, parser AST selection stamping, renderer selection consumption, UI controls, CLI/API validation, manifest assertions, and smoke coverage. Parser matching order was not changed except carrying output selection metadata after a block handles a node. Sample fixture expected Markdown did not change. blockOutputFamilyDefinitions/blockOutputFamilyOrder/getBlockOutputFamilyDefinition and selectionKey-less suffix fallback were removed, so the old block-type-only public contract is not partially retained. Residual risk is the intentional breaking change to output option keys and manifests.
