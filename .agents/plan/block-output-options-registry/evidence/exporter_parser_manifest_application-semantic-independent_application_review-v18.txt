NaverBlogExporter and SinglePostExport now pass options.blockOutputs into parsePostHtml together with markdown and resolveLinkUrl. BaseBlog selects the matching editor as before, and BaseEditor.runBlocks uses this.type plus the matched BaseBlock.outputOptions.blockId to create a stable Editor+Block key before resolving selectionKey through resolveBlockOutputSelection. Only selectable outputOptions with two or more variants are stamped onto AST blocks as outputSelectionKey/outputSelection, so parser matching is unchanged. MarkdownRenderer now honors block.outputSelection for code, formula, divider, image, and table before consulting defaults. Focused parser and renderer tests prove naver-se4:code and naver-se3:code render independently for the same code block type.
