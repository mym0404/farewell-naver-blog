pass: NaverBlog editor order is naver-se4, naver-se3, naver-se2 and getEditorBlockOutputDefinitions preserves that editor index and each editor supportedBlocks index. The registry filters out single-variant outputOptions, so heading, paragraph, quote, imageGroup, video, and linkCard are not selectable. The parser-block-registry test asserts the exact selectable key order: naver-se4 formula/code/table/image/divider, naver-se3 table/code/image, naver-se2 table/divider/code/image. Duplicate AST block types such as code are represented as naver-se4:code, naver-se3:code, and naver-se2:code, proving editor-specific grouping and independence.
