name: required-checks

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

jobs:
  validate:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Read Playwright version
        id: playwright-version
        run: |
          version=$(node -e "const fs = require('node:fs'); const pkg = JSON.parse(fs.readFileSync('node_modules/playwright/package.json', 'utf8')); console.log(pkg.version)")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Restore Playwright browser cache
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.version }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps --only-shell chromium

      - name: Run local checks
        run: pnpm check:local

      - name: Run UI smoke
        timeout-minutes: 10
        run: pnpm smoke:ui

      - name: Run parser block coverage gate
        run: pnpm test:parser-blocks

      - name: Check live upload secret
        id: live-upload-secret
        env:
          GOODBYE_UPLOAD_E2E_GITHUB_TOKEN: ${{ secrets.GOODBYE_UPLOAD_E2E_GITHUB_TOKEN }}
        run: |
          if [ -n "$GOODBYE_UPLOAD_E2E_GITHUB_TOKEN" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Write live upload .env
        if: ${{ steps.live-upload-secret.outputs.configured == 'true' }}
        env:
          GOODBYE_UPLOAD_E2E_GITHUB_TOKEN: ${{ secrets.GOODBYE_UPLOAD_E2E_GITHUB_TOKEN }}
        run: |
          cat <<EOF > .env
          GOODBYE_UPLOAD_E2E=1
          GOODBYE_UPLOAD_E2E_GITHUB_TOKEN=$GOODBYE_UPLOAD_E2E_GITHUB_TOKEN
          EOF

      - name: Run live upload Playwright e2e
        if: ${{ steps.live-upload-secret.outputs.configured == 'true' }}
        run: pnpm test:network:upload

      - name: Skip live upload Playwright e2e
        if: ${{ steps.live-upload-secret.outputs.configured != 'true' }}
        run: echo "GOODBYE_UPLOAD_E2E_GITHUB_TOKEN is not configured; skipping live upload Playwright e2e."

      - name: Run coverage gate
        run: pnpm test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          use_oidc: true
          files: ./coverage/lcov.info
          fail_ci_if_error: true
